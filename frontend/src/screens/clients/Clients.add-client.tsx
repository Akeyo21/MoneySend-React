import React from 'react';
import {View} from 'react-native';
import DateChange from './Clients.add-clitent.date';

const AddClient = () => {
  return (
    <View style={{padding: 30}}>
      <DateChange />
    </View>
  );
};

export default AddClient;
